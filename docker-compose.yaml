services:
  backend:
     # REMOVED: build: ./mern/backend
    # ADDED: Reference the image from Docker Hub
    image: ojayde35/mern-backend:latest # <--- IMPORTANT: Replace YOUR_DOCKERHUB_USERNAME
    ports:
      - "5050:5050" 
    networks:
      - mern_network
    environment:
      MONGO_URI: mongodb://mongo:27017/mydatabase  
    depends_on:
      - mongodb

  frontend:
    # REMOVED: build: ./mern/frontend
    # ADDED: Reference the image from Docker Hub
    image: ojayde35/mern-frontend:latest # <--- IMPORTANT: Replace YOUR_DOCKERHUB_USERNAME!!
    ports:
      - "5173:5173"  
    networks:
      - mern_network
    environment:
      REACT_APP_API_URL: http://backend:5050 

  mongodb:
    image: mongo:latest  
    ports:
      - "27017:27017"  
    networks:
      - mern_network
    volumes:
      - mongo-data:/data/db  

networks:
  mern_network:
    driver: bridge 

volumes:
  mongo-data:
    driver: local  # Persist MongoDB data locally

